'use client';

export default function Footer() {
    return(
        <div>
            This is a footer. It's only down here because the height of the Home Page Body is set. Remember to change this.
        </div>
    )
}