import { JetBrains_Mono as FontMono, Inter } from "next/font/google"
import { M_PLUS_1p, M_PLUS_Rounded_1c, Cairo, Cairo_Play } from "next/font/google"

export const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
})

export const fontMono = FontMono({
  subsets: ["latin"],
  variable: "--font-mono",
})